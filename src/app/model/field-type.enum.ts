export enum FieldType {
  text = 'text',
  textarea = 'textarea',
  checkbox = 'checkbox',
  date = 'date',
  email = 'email',
  tel = 'tel',
  range = 'range',
  number = 'number',
  select = 'select',
  form = 'form'
}
